import React from 'react';
import { mount } from 'enzyme';
import { Collapse } from 'antd';
import { barList, pieList } from '@charts';
import ComponentPanel from '../index';

const Panel = Collapse.Panel;

describe('<ComponentPanel />', () => {
  test('render', () => {
    // todo
  });
});